# Copy to .env (never commit), or set these variables in your host.
# The Android app never holds this key; it stays on the backend only.
GEMINI_API_KEY=your-google-ai-api-key
GEMINI_MODEL=gemini-3.6-flash