# This workflow has been disabled for security reasons (P1-06).
# Auto-approving PRs on production branches is a security risk.
# Please use manual review and branch protection rules instead.
name: Disabled Auto Approve
on:
  pull_request:
    branches: [ "master", "main" ]
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Auto-approve is disabled."
